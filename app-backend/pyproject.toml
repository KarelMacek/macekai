[project]
name = "app-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1,<6",
    "gunicorn>=23",
    "whitenoise>=6.8",
    "python-dotenv>=1.0",
    "psycopg[binary]>=3.2",
]

[tool.uv]
package = false
